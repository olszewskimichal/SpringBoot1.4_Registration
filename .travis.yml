language: java
jdk:
  - oraclejdk8

before_install:
  - chmod ugo+x mvnw

install: true
script: ./mvnw clean install

cache:
  directories:
  - $HOME/.m2

script:
  - ./mvnw clean install
  - bash <(curl -s https://codecov.io/bash)